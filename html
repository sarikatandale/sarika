

Use this element to enclose sample output from a computer program. It is usually displayed in the browser's default monotype font. *
This element does not maintain line breaks like <pre>
<samp>

This element encloses an abbreviation *
<abbr>

Use this element for text which should be displayed lower, and often smaller, than the main span of text. *
<lower>

Use this element to enclose a range of text that has been added to a document. *
<ins>

Use this element to highlight text. *
For example it can be used in a page showing search results to highlight every instance of the searched-for word.
<mark>

Use this element for side-comments and small print, including copyright and legal text, independent of its styled presentation. *

<small>

Do not use this element to increase the gap between lines of text; use the CSS margin property or the <p> element instead *
<spacer>

To strikethrough text with a line, you would enclose the text in this element. *
This element is not appropriate when indicating document edits; for that, use the <del> and <ins> elements, as appropriate.
<s>

This element is a generic inline container for content. *
This element does not have any semantic value. It should be used only when no other semantic element is appropriate. This element can be used to group content for styling purposes.
<span>

This element encloses a reference to a creative work. *
<cite>

Use this element to enclose either a time on a 24-hour clock or a precise date in the Gregorian calendar. *
<time>

Use this element to enclose a range of text that has been deleted from a document. *
This element is often (but need not be) rendered with strike-through text.
<del>
Use this element for text which should be displayed higher, and often smaller, than the main span of text. *
<sup>

Use this element to enclose a short quotation. *
This element is intended for short quotations that don't require paragraph breaks; for long quotations use <blockquote> element.
<q>

Use this tag to enclose a fragment of computer code. By default, it is displayed in the browser's default monospace font. Note: this tag does not preserve line breaks! *
<code>

<what tag goes here>Ctrl</what tag goes here> + <what tag goes here>S</what tag goes here> *
This element represents user input and produces an inline element displayed in the browser's default monospace font.
<kbd>

